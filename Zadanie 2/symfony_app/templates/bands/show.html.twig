{% extends 'base.html.twig' %}

{% block title %}Bands{% endblock %}

{% block body %}
    <h1>Bands</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ band.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ band.name }}</td>
            </tr>
            <tr>
                <th>Discs</th>
                <td>{{ band.discs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bands_index') }}">back to list</a>

    <a href="{{ path('app_bands_edit', {'id': band.id}) }}">edit</a>

    {{ include('bands/_delete_form.html.twig') }}
{% endblock %}
